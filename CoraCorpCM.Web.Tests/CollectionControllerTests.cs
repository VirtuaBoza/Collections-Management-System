using Microsoft.VisualStudio.TestTools.UnitTesting;
using Moq;
using CoraCorpCM.Data;
using CoraCorpCM.Domain;
using Microsoft.AspNetCore.Identity;
using System.Security.Claims;

namespace CoraCorpCM.Web.Tests
{
    [TestClass]
    public class CollectionControllerTests
    {
        [TestMethod]
        public void GetsIndex_WhileLoggedIn_ListsPieces()
        {
            // Arrange
            //var 
            //var user = new ApplicationUser { Museum = }
            //var mockUserManager = new Mock<UserManager<ApplicationUser>>();
            //mockUserManager.Setup(u => u.GetUserAsync(It.IsAny<ClaimsPrincipal>())).Returns()

            //var mockRepo = new Mock<IMuseumRepository>();
            //mockRepo.Setup(r => r.GetMuseum())

            // Act


            // Assert

        }
    }
}
